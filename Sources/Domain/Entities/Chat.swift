import Foundation

// NOTE: Channel, Message, Reaction, ChatChannelType types are defined
// in Presentation/Features/Chat/ChatViewModel.swift (presentation-layer models).
// This file previously held domain entity duplicates which have been removed
// to avoid type ambiguity.
//
// If you need domain-specific chat models, define them here with a
// "Domain" prefix (e.g., DomainChannel) and do not duplicate names.
