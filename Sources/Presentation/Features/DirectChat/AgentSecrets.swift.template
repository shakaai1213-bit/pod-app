// AgentSecrets.swift.template — COMMITTED template; copy to AgentSecrets.swift
//
// Setup steps for a fresh checkout:
//   1. cp AgentSecrets.swift.template AgentSecrets.swift
//   2. Replace each REPLACE_ME below with the live token
//   3. Remove the #error line below
//
// AgentSecrets.swift is gitignored — never commit it.
// If you accidentally commit the .swift file with real tokens, immediately
// rotate the tokens (Path A per Rooster M1 security review 2026-05-07) —
// committed = compromised. Scrubbing git history alone is not enough.
//
// This template file is not compiled by Xcode (extension is .swift.template,
// not .swift). The #error below exists as a SAFETY NET: if a future developer
// renames this file to AgentSecrets.swift without filling in real values, the
// build fails immediately with a clear message instead of producing a broken
// binary that silently fails auth at runtime.
//
// Required addition per Rooster M1 security review Decision 1 (2026-05-07).

#error("AgentSecrets.swift not configured. Copy AgentSecrets.swift.template to AgentSecrets.swift, fill in the real token values, and remove this #error line. See top-of-file comments for setup steps.")

import Foundation

enum AgentSecrets {
    /// Gateway token for shaka-mac-hosted agents (Aurora, Maui, Aloha, Coral, Shaka).
    /// Reaches the OpenClaw gateway at shakas-mac-mini.tail82d30d.ts.net.
    static let shakaMacGatewayToken = "REPLACE_ME_SHAKA_MAC_GATEWAY_TOKEN"

    /// Gateway token for chief-mac-hosted agents (Luna, Chief, Rooster, Reef).
    /// Reaches the OpenClaw gateway at 100.80.44.41:18789.
    static let chiefMacGatewayToken = "REPLACE_ME_CHIEF_MAC_GATEWAY_TOKEN"
}
